Description: pro-actively disable LD_AUDIT for setuid binaries.
Author: Kees Cook <kees@ubuntu.com>

Index: glibc-2.9/elf/rtld.c
===================================================================
--- glibc-2.9.orig/elf/rtld.c	2010-10-21 14:33:21.002139173 -0700
+++ glibc-2.9/elf/rtld.c	2010-10-21 14:33:26.802483579 -0700
@@ -2465,7 +2465,7 @@
   while ((p = (strsep) (&str, ":")) != NULL)
     if (p[0] != '\0'
 	&& (__builtin_expect (! INTUSE(__libc_enable_secure), 1)
-	    || strchr (p, '/') == NULL))
+	))
       {
 	/* This is using the local malloc, not the system malloc.  The
 	   memory can never be freed.  */
